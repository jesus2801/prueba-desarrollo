const FileLi = () => {
  return (
    <h1>asdf</h1>
    // <FileLiStyles
    //   className={
    //     selected
    //       ? selected.fileId === data.fileId
    //         ? 'selected'
    //         : 'no-selected'
    //       : 'no-selected'
    //   }
    //   onClick={() => dispatch(setSelectedFile(data))}
    // >
    //   {data.name}
    // </FileLiStyles>
  );
};

export default FileLi;
